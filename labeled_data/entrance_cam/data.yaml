# Dataset paths should be relative to the data.yaml file location
path: .  # root directory is the current directory
task: detect # task type
train: train/images  # path to training images
val: val/images     # path to validation images
nc: 1  # number of classes (bee_alive, bee_dead and feeder)
# Class names
names: ['bee']  # class names